function Industrycard(){
    const listIndustry = routing.map((industry) =>
        <div key={industry.name} className="bg-white rounded-lg shadow-2xl">
            <div className="p-8">
                {industry.name}
            </div>
            <div className="flex space-x-2 items-center justify-center p-2">
                <Route
                    key={industry.name}
                    path={industry.path}
                    component={industry.component}

                >
                    Explore Now
                </Route>
            </div>
        </div>

    );
    return (
        <div className="grid grid-cols-1 md:grid-cols-3 gap-x-2 gap-y-4">
            {listIndustry}
        </div>
    );
}